# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Current State

This repository (RDAI) is newly created and does not yet contain any source code, build configuration, or documentation. There is no build system, test suite, or linter configured yet.

## Guidance for Working in This Repository

- Because the repository is empty, there are no established conventions to follow yet. When adding the first code, choose the toolchain and layout deliberately, since it will set the precedent for the project.
- Update this file as the project takes shape. At minimum, once code exists, document here:
  - **Commands**: how to install dependencies, build, run tests (including a single test), and lint.
  - **Architecture**: the high-level structure and how the major pieces fit together — the things that require reading multiple files to understand.
  - **Conventions**: any project-specific patterns that differ from language/framework defaults.
